{
  "tool": "gokit-scaffold",
  "version": "{{ .Version }}",
  "template_pack": "service-http",
  "spec": {
    "name": "{{ .Name }}",
    "module": "{{ .Module }}",
    "http_port": {{ .HTTPPort }}
  }
}
